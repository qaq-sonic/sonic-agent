sonic:
  agent:
    # Replace with the key of agent generated by the new front-end | 替换为前端新增Agent生成的key
    key: ${AGENT_KEY:2a59097b-7662-48ae-b04c-f701d9faf0b2}
  server:
    wss: wss://qaq.openqaq.fun/api/sonic/agent/${sonic.agent.key}
